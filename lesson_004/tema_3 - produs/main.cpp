#include <iostream>

using namespace std;

int main()
{

    int a,b,c,d,e;

    cout << "Introduceti nr a,b,c,d,e!" << endl;

    cin >> a >> b >> c >> d >> e;

    cout << "Afiseaza a + 2*b + 3*c + 4*d + 5*e = " << a  + 2 * b + 3 * c + 4 * d + 5 * e << endl;

    return 0;
}
